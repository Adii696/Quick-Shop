# QuickShop Re-Advanced

Features: admin dashboard, analytics, image upload, mock checkout, docker, seeder, improved UI.

Run backend: cd backend && npm install && cp .env.sample .env && node seed.js && npm run dev
Run frontend: cd frontend && npm install && npm start
Docker: docker compose up --build
